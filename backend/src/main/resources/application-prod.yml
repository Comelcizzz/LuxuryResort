app:
  cors:
    allowed-origins: ${CORS_ORIGINS:https://luxuryresort-web.onrender.com,http://localhost:5173}
  jwt:
    secret: ${JWT_SECRET}

spring:
  flyway:
    locations: classpath:db/migration

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
